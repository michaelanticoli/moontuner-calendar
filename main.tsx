
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import MoontunerContentCalendar from './MoontunerContentCalendar';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MoontunerContentCalendar />
  </React.StrictMode>
);
